# Cloudflare Workers + static assets — required so CI `npx wrangler deploy` succeeds.
# (Dashboard is locked to that command; `wrangler pages deploy` is not used.)
name = "warthundercheats"
main = "workers/site.js"
compatibility_date = "2026-09-12"

# Cloudflare CI invokes `npx wrangler deploy` directly. Build Astro first so
# the static-assets directory exists before Wrangler validates it.
[build]
command = "npm run build"

[[routes]]
pattern = "warthundercheats.xyz"
custom_domain = true

[[routes]]
pattern = "www.warthundercheats.xyz"
custom_domain = true

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "404-page"
html_handling = "drop-trailing-slash"
# Sitemap/robots are static assets (dayzcheats-proven path for Googlebot).
# Worker still handles everything else first (www→apex, HTML charset).
run_worker_first = [
  "/*",
  "!/sitemap.xml",
  "!/sitemap.txt",
  "!/robots.txt",
  "!/sitemap.css",
  "!/favicon.svg",
  "!/_astro/*",
  "!/media/*",
  "!/videos/*",
  "!/og/*",
]
